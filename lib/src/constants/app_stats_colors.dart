import 'package:flutter/material.dart';

class AppStatsColors {
  // Background (Card)
  static const relaxBg = Color(0xFFDBEAFE);
  static const workBg  = Color(0xFFFEF3C7);
  static const walkBg  = Color(0xFFD1FAE5);
  static const fallsBg = Color(0xFFFCA5A5);

  // Primary Activity Color (Donut + Weekly)
  static const relaxPrimary = Color(0xFF60A5FA); // soft blue
  static const workPrimary  = Color(0xFFFBBF24); // soft amber
  static const walkPrimary  = Color(0xFF34D399); // mint green
  static const fallsPrimary = Color(0xFFEF4444); // soft red

  // Text
  static const defaultText = Color(0xFF4B5563);
  static const dangerText  = Color(0xFF7F1D1D);
}
